describe("Criar categoria", () => {
  it("Espero que 2+2 seja 4", () => {
    const sum = 2 + 2;
    const res = 4;

    expect(sum).toBe(res);
  });

  it("Espero que 2+2 não seja 5", () => {
    const soma = 2 + 2;
    const naoquerer = 5;

    expect(soma).not.toBe(naoquerer);
  });
});
